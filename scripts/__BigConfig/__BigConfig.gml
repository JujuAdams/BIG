//Each word is represents a 32-bit integer (though they're actually stored as 64-bit)
//6 words is thus a 192-bit integer, or 24 bytes overall
//Increase this number if you need to handle larger numbers
#macro BIG_MAX_WORDS  6